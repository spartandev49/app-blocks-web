name: Navigation contrast branch snapshot

on:
  push:
    branches:
      - fix/navigation-contrast-v0.4.2
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upload repository snapshot
        uses: actions/upload-artifact@v4
        with:
          name: navigation-contrast-branch-snapshot
          path: .
          include-hidden-files: true
          retention-days: 1
