name: Generation 2 and Motion 3

on:
  pull_request:
  push:
    branches:
      - feature/motion-engine-v03
      - main

permissions:
  contents: read

jobs:
  verify:
    name: Node ${{ matrix.node }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22, 24]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Environment
        run: |
          node --version
          npm --version
      - name: Syntax
        run: |
          node --check bin/appblocks-v2.js
          node --check src/generation2.js
          node --check src/normalizer-v2.js
          node --check src/normalizer-v3.js
          node --check src/generation2-assets.js
          node --check src/motion3.js
          node --check src/compiler-v2.js
          node --check src/compiler-v3.js
          node --check src/parser-v2.js
      - name: Generation 2 and Motion 3 tests
        run: node --test test/generation2.test.js test/generation2-token-budget.test.js test/generation2-cli.test.js test/motion3.test.js
      - name: Release tests
        run: |
          set -o pipefail
          node --test test/generation2-release.test.js 2>&1 | tee release-tests-node-${{ matrix.node }}.log
      - name: Existing test suite
        run: |
          set -o pipefail
          npm test 2>&1 | tee full-tests-node-${{ matrix.node }}.log
      - name: Upload diagnostic logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: motion3-test-logs-node-${{ matrix.node }}
          path: |
            release-tests-node-${{ matrix.node }}.log
            full-tests-node-${{ matrix.node }}.log
          if-no-files-found: ignore
      - name: Strict Generation 2 build
        run: node bin/appblocks-v2.js build examples/generation2-showcase.ab --out .verification/generation2 --strict
      - name: Strict Motion Engine 3 build
        run: node bin/appblocks-v2.js build examples/motion-showcase.ab --out .verification/motion --strict
      - name: Generated-output audit
        run: npm run --if-present audit
      - name: Benchmark
        run: npm run --if-present benchmark
      - name: Package inspection
        run: npm pack --dry-run
